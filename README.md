# Devopsproject--story-1
The repository is structured with directories for infrastructure (infra) and application (app) code.
Terraform configurations are added to the infra directory.
Kubernetes manifests and Helm charts are added to the app directory.
